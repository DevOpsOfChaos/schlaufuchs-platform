---
import BaseLayout from "../../../../../layouts/BaseLayout.astro";
import { withBase } from "../../../../../utils/base";

const targetHref = withBase("fachbereiche/linux/system");
---

<BaseLayout title="Benutzer & Gruppen – Fachinfo" description="Die alte Fachseite wird auf die aktuelle Themenübersicht weitergeleitet.">
  <script is:inline define:vars={{ targetHref }}>
    window.location.replace(targetHref);
  </script>

  <section>
    <h1>Benutzer & Gruppen</h1>
    <p>Die aktuelle Fachübersicht wird geöffnet.</p>
    <a href={targetHref}>Zur Themenübersicht</a>
  </section>
</BaseLayout>
