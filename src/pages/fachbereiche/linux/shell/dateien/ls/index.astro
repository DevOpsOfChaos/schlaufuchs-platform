---
import BaseLayout from "../../../../../../layouts/BaseLayout.astro";
import ArticleLayout from "../../../../../../components/content/ArticleLayout.astro";
import { getCollection, render } from "astro:content";

const articleId = "linux/shell/dateien/ls";
const articles = await getCollection("articles");
const entry = articles.find((item) => item.id.replace(/\.(md|mdx)$/i, "") === articleId);

if (!entry) {
  throw new Error(`Article not found for: linux/shell/dateien/ls`);
}

const { Content, headings } = await render(entry);
const breadcrumbs = [
  { label: "Start", href: "/" },
  { label: "Linux", href: "/fachbereiche/linux" },
  { label: "Linux Shell", href: "/fachbereiche/linux/shell" },
  { label: "Dateien", href: "/fachbereiche/linux/shell/dateien" },
  { label: "ls verstehen", href: "/fachbereiche/linux/shell/dateien/ls" },
];
const prevLink = null;
const nextLink = { title: "Dateien und Ordner", href: "/fachbereiche/linux/shell/dateien/dateien-und-ordner" };
---

<BaseLayout title={`${entry.data.title} – Schlaufuchs`} description={entry.data.description}>
  <ArticleLayout
    entry={entry}
    Content={Content}
    headings={headings}
    breadcrumbs={breadcrumbs}
    prevLink={prevLink}
    nextLink={nextLink}
  />
</BaseLayout>
