---
import BaseLayout from "../../../../../layouts/BaseLayout.astro";
import { withBase } from "../../../../../utils/base";

const targetHref = withBase("fachbereiche/web-development/css");
---

<BaseLayout title="Flexbox – Fachinfo" description="Die alte Fachseite wird auf die aktuelle Themenübersicht weitergeleitet.">
  <script is:inline define:vars={{ targetHref }}>
    window.location.replace(targetHref);
  </script>

  <section>
    <h1>Flexbox</h1>
    <p>Die aktuelle Fachübersicht wird geöffnet.</p>
    <a href={targetHref}>Zur Themenübersicht</a>
  </section>
</BaseLayout>
