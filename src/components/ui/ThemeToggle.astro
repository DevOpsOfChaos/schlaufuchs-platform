---
import ActionIcon from "./ActionIcon.astro";

const sunIcon = `
<svg viewBox="0 0 24 24" aria-hidden="true">
  <circle cx="12" cy="12" r="4.2"></circle>
  <path d="M12 2.5v2.2M12 19.3v2.2M21.5 12h-2.2M4.7 12H2.5M18.7 5.3l-1.5 1.5M6.8 17.2l-1.5 1.5M18.7 18.7l-1.5-1.5M6.8 6.8L5.3 5.3"></path>
</svg>`;

const moonIcon = `
<svg viewBox="0 0 24 24" aria-hidden="true">
  <path d="M19 14.8A7.8 7.8 0 0 1 9.2 5a8.8 8.8 0 1 0 9.8 9.8Z"></path>
</svg>`;
---

<div class="theme-toggle-shell" data-theme-toggle>
  <div class="theme-light">
    <ActionIcon
      label="Light Mode"
      tooltip="Heller Modus"
      icon={sunIcon}
      type="button"
    />
  </div>
  <div class="theme-dark">
    <ActionIcon
      label="Dark Mode"
      tooltip="Dunkler Modus"
      icon={moonIcon}
      type="button"
    />
  </div>
</div>

<script>
  const root = document.documentElement;
  const shell = document.querySelector("[data-theme-toggle]");
  if (shell) {
    const button = shell.querySelector("button");
    const render = () => {
      const isDark = root.dataset.theme === "dark";
      shell.classList.toggle("is-dark", isDark);
      shell.classList.toggle("is-light", !isDark);
      if (button) {
        button.setAttribute("aria-label", isDark ? "Light Mode aktivieren" : "Dark Mode aktivieren");
        button.setAttribute("data-tooltip", isDark ? "Zu Light Mode wechseln" : "Zu Dark Mode wechseln");
      }
    };

    render();

    button?.addEventListener("click", () => {
      const next = root.dataset.theme === "dark" ? "light" : "dark";
      root.dataset.theme = next;
      try {
        localStorage.setItem("schlaufuchs-theme", next);
      } catch {}
      render();
      window.dispatchEvent(new CustomEvent("theme-changed", { detail: next }));
    });
  }
</script>

<style>
  .theme-toggle-shell {
    display: contents;
  }

  .theme-light,
  .theme-dark {
    display: none;
  }

  .theme-toggle-shell.is-light .theme-light,
  .theme-toggle-shell.is-dark .theme-dark {
    display: contents;
  }
</style>
