# Status v204

v204 erweitert die neue kumulative Linie ab v197 um Web-Projektpraxis, Build, QA, Release, Monitoring und Wartbarkeit.

Validierungsziel: keine Aufgabe ohne Lösung, eindeutige `taskId`, passende `relatedExercise`, parsebare SVGs.
